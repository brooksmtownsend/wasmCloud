# control-interface-proto

## Prerequisites

- proto

## rust-analyzer

It's recommended to set the following configuration option in your rust-analyzer configuration to ensure the build script runs:

```json
{
  "rust-analyzer.cargo.buildScripts.enable": true
}
```
